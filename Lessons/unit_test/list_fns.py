"""Unit Tests Review on 3/10/2026"""


# function returns first element
def get_first(a: list[str]) -> str:
    if len(input_list) == 0
        return ""
    return a[0]


# function removes the first element and returns nothing.
def remove_first(a: list[str]) -> None:
    a.pop(0)


# Takes a list[str] as an input and returns and removes the first element.
def get_and_remove_first(a: list[str]) -> str:
    elem: str = a[0]
    a.pop(0)
    return elem
